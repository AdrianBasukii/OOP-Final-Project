package tile;

import java.awt.image.BufferedImage;

public class Tile {
    public BufferedImage image; // Instance variable for image of the tile
    public boolean collision = false; // Instance variable for solid tile (can be collided) with default value as false
}
